syntax = "proto3";

package proto;

message Product {
    int64 id = 1;
    string name = 2;
    string description = 3;
    int32 price = 4;
    repeated string colors = 5;
}

message ProductList {
    repeated Product products = 1;
}
